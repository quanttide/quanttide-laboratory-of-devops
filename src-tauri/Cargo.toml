[package]
name = "kse-tauri"
version = "0.1.0"
edition = "2021"

[dependencies]
kse_core = { path = ".." }
tauri = { version = "1", features = ["shell-open"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-build = { version = "1", features = [] }
